/**
 * Toast notifications via Sonner — use anywhere in client components.
 *
 * @example
 * import { toast } from "@/lib/toast";
 * toast.success("PRD saved");
 * toast.error("Something went wrong");
 */
export { toast } from "sonner";
